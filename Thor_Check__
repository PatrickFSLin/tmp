else:
    traj_store = np.asarray(
        trajectories,
        dtype=np.float32
    ).reshape(args.num_trajectories, 6, 2)

    ade_store = np.asarray(
        ades,
        dtype=np.float32
    ).reshape(args.num_trajectories)

    predictions_all[local_idx, :, :, :] = traj_store
    ade_all[local_idx, :] = ade_store
