print("DEBUG trajectories type:", type(trajectories))
print("DEBUG trajectories shape:", np.asarray(trajectories).shape)
print("DEBUG sliced shape:", np.asarray(trajectories)[:, 0, :, :].shape)
print("DEBUG reshaped shape:", np.asarray(trajectories).reshape(args.num_trajectories, 6, 2).shape)
print("DEBUG target shape:", predictions_all[local_idx].shape)
